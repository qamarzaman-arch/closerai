{
  "name": "closer-ai",
  "version": "1.0.0",
  "description": "Enterprise AI Sales Copilot for Real Estate",
  "scripts": {
    "install:all": "npm install && cd apps/server && npm install && cd ../electron && npm install",
    "dev:server": "cd apps/server && npm run dev",
    "dev:electron": "cd apps/electron && npm run dev",
    "build:all": "cd apps/server && npm run build && cd ../electron && npm run build",
    "start": "concurrently \"npm run dev:server\" \"npm run dev:electron\"",
    "test": "cd apps/server && npm run test"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
